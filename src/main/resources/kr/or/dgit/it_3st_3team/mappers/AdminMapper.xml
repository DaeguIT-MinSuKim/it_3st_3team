<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.it_3st_3team.dao.AdminDao">
	<resultMap type="Admin" id="AdminBasedResult">
		<id property="adminNo" column="adm_no" />
		<result property="adminId" column="adm_id" />
		<result property="adminName" column="adm_id" />
		<result property="adminPwd" column="adm_id" />
		<result property="phone" column="adm_phone" />
	</resultMap>
	
	<resultMap type="AdminGroup" id="AdminWithAdmin_groupResult" extends="AdminBasedResult">
		<result property="adminGroup.agNo" column="ag_no"/>
		<result property="adminGroup.agName" column="ag_name"/>
		<result property="adminGroup.agAuth" column="ag_auth"/>
	</resultMap>
	
	<!-- Admin 기본 정보 -->
	<sql id="selectSql">
		select adm_no, adm_name, adm_id, adm_pwd, adm_phone, ag_no from admin
	</sql>
	
	<!-- Admin with Admin_group 정보 -->
	<sql id="selectWtihAdminGroupSql">
		select adm_no, adm_name, adm_id, adm_pwd, adm_phone, ag_no from admin ad join admin_group ag
		on ag.ag_no=ad.adm_no;
	</sql>
	
	
	
	<sql id="loginWhereSql">
		<where>
			<if test="adminId != null">
				adm_id = #{adminId}
			</if>
			<if test="adminPwd != null">
				AND adm_pwd = password(#{adminPwd}) 
			</if>
		</where>
	</sql>
	
	<sql id="whereSql">
		<where>
			<if test="adminNo != null">
				adm_no = #{adminNo}
			</if>
		</where>
	</sql>
	
	<select id="selectLogin" parameterType="Admin" resultMap="AdminBasedResult">
		<include refid="selectSql"/>
		<include refid="loginWhereSql"/>
	</select>
	
	<select id="selectAdminByNo" parameterType="Admin" resultMap="AdminBasedResult">
		<include refid="selectSql" />
		<include refid="whereSql" />
	</select>
</mapper>